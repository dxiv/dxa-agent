// Open-build stub — BYOC runner is bundled only when feature('BYOC_ENVIRONMENT_RUNNER') is on.
export async function environmentRunnerMain(_args: string[]): Promise<void> {
  throw new Error('Environment runner is unavailable in the open build.')
}
