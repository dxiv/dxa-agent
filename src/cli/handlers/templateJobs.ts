// Open-build stub — template jobs are bundled only when feature('TEMPLATES') is on.
export async function templatesMain(_args: string[]): Promise<void> {
  throw new Error('Template jobs are unavailable in the open build.')
}
