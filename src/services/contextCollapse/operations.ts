import type { Message } from '../../types/message.js'

/** Stub: real build replays collapse commits onto the API-bound message list. */
export function projectView(messages: Message[]): Message[] {
  return messages
}
