# Optional helpers under python/ — not required for the main Deimos CLI.
# Install:  pip install -r python/requirements.txt
#
# Python 3.11+ recommended (3.12+ preferred). Floors track PyPI as of April 2026.

httpx>=0.28.1,<1
pytest>=9.0.3,<10
pytest-asyncio>=1.3.0,<2
